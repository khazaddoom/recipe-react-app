import React from 'react';
const Card = () => {
    return (
      <div>
        <button onClick={() => this.setState({liked: true})}>Like</button>
      </div>
    );
  }
  
  export default Card;
  